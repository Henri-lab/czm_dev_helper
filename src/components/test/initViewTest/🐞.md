出现 bug 的代码~🚨
````javascript
// 初始化 czm viewer
// --为 app 安装插件的时机放在组件挂载的时期 而非在 main 立马执行
import app from '?main';
import { czmViewPlugin } from '?czmViewPlugin';
const cesiumContainer = ref(null);
app.use(czmViewPlugin);
let el = cesiumContainer.value;
if (el) {
// 将生成的 DOM 元素移动到 Vue 模板中的 cesiumContainer 中
const cV = document.getElementById('czm-viewer');
const cC = document.getElementById('czm-container');
if (cC && cV) {
el.appendChild(cC); //📍 red~~
cC.appendChild(cV);
}
}
````
